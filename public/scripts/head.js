function onPageLoad() {
    var sunStatusCanvas = $("#sunStatusCanvas");
    if (sunStatusCanvas) {
        drawSunStatusCanvas(sunStatusCanvas);
    }
}

function drawSunStatusCanvas(canvas) {
    console.log("These is sun status canvas!");
}